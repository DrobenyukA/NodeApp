<form method="POST" action="<%= `${action}` %>" class="btn-wrapper">
    <input type="hidden" name="_csrf" value="<%= csrfToken %>"/>
    <input type="hidden" name="productId" value="<%= id %>"/>
    <button class="btn danger" type="submit">Delete</button>
</form>